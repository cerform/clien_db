# 🚀 Краткое руководство быстрого старта

## За 5 минут до запуска бота!

### ⚡ Шаг 1: Клонирование и установка (1 минута)

**Windows:**
```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
```

**Linux/Mac:**
```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

Или используйте автоматический скрипт:
- **Windows:** `setup.bat`
- **Linux/Mac:** `bash setup.sh`

### 🤖 Шаг 2: Получение Telegram Bot Token (1 минута)

1. Откройте Telegram и найдите **@BotFather**
2. Напишите `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен вида: `123456789:ABCDefGhIjKlMnOpQrStUvWxYz`

### 🔑 Шаг 3: Настройка Google API (2 минуты)

#### Скачайте credentials.json:
1. Перейдите на https://console.cloud.google.com
2. Создайте новый проект
3. Включите:
   - ✅ **Google Sheets API**
   - ✅ **Google Calendar API**
4. Создайте **Service Account** (IAM & Admin → Service Accounts)
5. Создайте ключ (JSON) и скачайте
6. Сохраните как **`credentials.json`** в корне проекта

#### Создайте Google Spreadsheet:
1. Перейдите на https://sheets.google.com
2. Создайте новую таблицу
3. Скопируйте ID из URL: `https://docs.google.com/spreadsheets/d/{ID}/edit`

#### Раздайте доступ:
1. Откройте `credentials.json`
2. Найдите `client_email`
3. Откройте Spreadsheet и раздайте доступ этому email (как "Editor")

### ⚙️ Шаг 4: Конфигурация бота (1 минута)

Запустите бот:
```bash
python src/main.py
```

Выберите в меню: **1. ⚙️  Конфигурация**

Введите:
1. **Bot Token** → вставьте токен от BotFather
2. **Spreadsheet ID** → вставьте ID таблицы
3. **Credentials** → выберите вариант с credentials.json
4. **Timezone** → выберите `Europe/Moscow` (или свой)
5. **Admin IDs** → найдите свой ID у @userinfobot

Сохраните конфигурацию.

### 📊 Шаг 5: Инициализация базы (30 секунд)

```bash
python init_db.py
```

Это создаст необходимые листы в Google Sheets.

### ✅ Готово! Запустите бота

```bash
python src/main.py
```

Выберите: **2. ✅ Запустить бота**

Бот готов к работе! 🎉

---

## 🔍 Проверка проблем

### ❌ Ошибка: "TELEGRAM_BOT_TOKEN not set in .env"
- Проверьте, что вы ввели токен в конфигурации
- Убедитесь, что `.env` файл существует

### ❌ Ошибка: "Google credentials file not found"
- Убедитесь, что `credentials.json` находится в корне проекта
- Проверьте путь в конфигурации

### ❌ Ошибка: "Permission denied" на Google Sheets
- Откройте Spreadsheet и раздайте доступ Service Account email
- Refresh и попробуйте снова

### ❌ Бот не отвечает
- Проверьте токен у @BotFather
- Убедитесь, что интернет-соединение активно
- Посмотрите логи в `logs/bot.log`

---

## 📱 Первое использование в Telegram

1. Найдите вашего бота в Telegram
2. Нажмите `/start`
3. Заполните профиль (имя, телефон)
4. Используйте меню!

---

## 👨‍💻 Для администраторов

Ваш User ID уже установлен в конфигурации. Вы можете:
- Управлять клиентами
- Просматривать статистику
- Управлять мастерами

---

## 📚 Дополнительные ресурсы

- 📖 **Полная документация** → `README.md`
- 💡 **Примеры кода** → `examples.py`
- 🔧 **Конфигурация** → `.env` файл
- 📋 **Логи** → `logs/bot.log`

---

## 🎯 Что дальше?

- Добавьте мастеров в Google Sheets (лист "masters")
- Протестируйте запись на прием
- Настройте Google Calendar для каждого мастера
- Подключите SMS/Email уведомления (опционально)

---

**Вопросы?** Проверьте логи: `logs/bot.log`
