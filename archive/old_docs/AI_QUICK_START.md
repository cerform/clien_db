# 🚀 Быстрый старт AI бота

## Что изменилось

Бот теперь **полностью работает через AI**:
- ✅ Понимает естественный язык (русский, английский, иврит)
- ✅ Не нужны кнопки и команды
- ✅ Ведёт полноценный диалог
- ✅ Выполняет административные функции
- ✅ Управляет записями и расписанием

## Быстрая установка

### 1. Обновите зависимости

```bash
cd tattoo_appointment_bot
pip install --upgrade -r requirements.txt
```

### 2. Проверьте .env

Убедитесь что есть OpenAI API ключ:

```bash
OPENAI_API_KEY=sk-your-key-here
```

### 3. Запустите бота

```bash
python run.py
```

## Как это работает

### Для клиентов

Просто пишите естественным языком:

```
Привет! Хочу записаться на тату
Когда есть свободное время?
Запиши меня на 15 декабря в 14:00
```

### Для администраторов

Управляйте через диалог:

```
Покажи все записи на завтра
Добавь свободное время 20 декабря с 15:00 до 19:00
Какая статистика за неделю?
```

## Команды

- `/start` - Начать
- `/help` - Помощь
- `/reset` - Очистить историю
- `/admin` - Админ панель (для админов)

## Структура

Новые файлы:
- `src/services/ai_dialog_engine.py` - AI движок (GPT-4)
- `src/services/ai_orchestrator.py` - Связь с сервисами
- `src/services/service_factory.py` - Фабрики сервисов
- `src/bot/handlers/ai_handler.py` - Обработчик сообщений
- `AI_BOT_README.md` - Полная документация

## Проблемы?

1. **AI не отвечает**: Проверьте OPENAI_API_KEY
2. **Ошибки импорта**: `pip install --upgrade openai`
3. **Не работают действия**: Проверьте Google Sheets подключение

## Откат на старую версию

Если нужно вернуться к кнопкам, в `src/bot/router.py`:

```python
# Закомментируйте
# ai_handler.setup(dp)

# Раскомментируйте
multilingual_handler.setup(dp)
client_handlers.setup(dp)
admin_handlers.setup(dp)
```

---

**Готово! Бот работает полностью через AI 🤖**

Подробная документация: `AI_BOT_README.md`
