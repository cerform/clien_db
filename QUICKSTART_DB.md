# ⚡ БЫСТРЫЙ СТАРТ - БД готова к использованию!

## 🎯 За 5 минут

### Шаг 1: Откройте таблицу
```
https://docs.google.com/spreadsheets/d/1gU-8cqVOiGHcgY823vQeC6JX8oZfLOrJ_l7OjRqqHiY/edit
```

### Шаг 2: Проверьте листы
- ✅ Мастера (4 мастера)
- ✅ Клиенты (3 клиента)
- ✅ Услуги (6 услуг)
- ✅ Записи (пуста)
- ✅ Расписание (пуста)
- ✅ Отзывы (пуста)
- ✅ Прайс-лист (пуста)

### Шаг 3: Добавьте свои данные
Просто отредактируйте таблицу в Google Sheets или используйте код:

```python
from src.db.sheets_client import GoogleSheetsClient
from src.config.config import get_config

config = get_config()
client = GoogleSheetsClient(
    credentials_file=config.google_credentials_json,
    spreadsheet_id=config.google_spreadsheet_id
)

# Добавить мастера
client.append_row("Мастера", [
    '5', 'Ваше имя', 'Специальность', '10', '5.0', 
    '+7-999-999-99-99', '@instagram', '5000', 'active', 'Лучший мастер'
])
```

## 📚 Что находится где

### Мастера
```
ID | Имя | Специальность | Опыт | Рейтинг | Телефон | Instagram | Цена | Статус | Описание
```
**Примеры:**
- Алексей Волков - Портреты (4.9★)
- Мария Петрова - Минимализм (4.8★)
- Иван Сидоров - Цветная классика (5.0★)
- Елена Морозова - Орнаменты (4.7★)

### Клиенты
```
ID | Telegram ID | Имя | Телефон | Email | Дата регистрации | Статус | Визитов | Потрачено | Примечания
```
**Примеры:**
- Петр Иванов (3 визита, 8500 руб)
- Ольга Сергеева (1 визит, 3500 руб)
- Дмитрий Козлов (2 визита, 5000 руб)

### Услуги
```
ID | Название | Описание | Длительность | Цена | Категория | Статус | Изображение
```
**Примеры:**
- Портретное тату (120 мин, 3500 руб)
- Минималистичное тату (60 мин, 2500 руб)
- Цветное классическое тату (90 мин, 4000 руб)
- Орнамент/Мандала (150 мин, 3000 руб)
- Маленькое тату (30 мин, 1500 руб)
- Консультация (30 мин, 500 руб)

## 🔧 Основные операции

### Добавить запись
```python
client.append_row("Записи", [
    '10001', '123456789', 'Петр Иванов', '987654321', 'Алексей Волков',
    'Портретное тату', '10.12.2025 14:30', '120', '3500', 'confirmed',
    '03.12.2025 10:00', 'Согласован'
])
```

### Добавить отзыв
```python
client.append_row("Отзывы", [
    '1', '123456789', 'Петр Иванов', '987654321', 'Алексей Волков',
    '5', 'Отличная работа! Очень доволен!', '03.12.2025', '10001', '5'
])
```

### Добавить в расписание
```python
client.append_row("Расписание", [
    '1', '987654321', 'Алексей Волков', '10.12.2025', '10:00', '18:00',
    'Свободно', 'work', ''
])
```

### Получить все записи
```python
rows = client.get_sheet_values("Мастера")
for row in rows[1:]:
    print(f"{row[1]} - {row[2]}")
```

### Найти строку
```python
row_num = client.find_row("Мастера", 0, '1')  # Найти ID "1"
if row_num:
    print(f"Найдена на строке {row_num}")
```

### Обновить строку
```python
client.update_row("Мастера", 2, [
    '1', 'Алексей Волков', 'Новая специальность', '8', '4.95', ...
])
```

### Удалить строку
```python
client.delete_row("Мастера", 2)
```

## 🎯 Примеры для бота

### Получить всех мастеров для кнопок
```python
@router.message(Command("masters"))
async def show_masters(message: Message):
    config = get_config()
    client = GoogleSheetsClient(
        credentials_file=config.google_credentials_json,
        spreadsheet_id=config.google_spreadsheet_id
    )
    
    rows = client.get_sheet_values("Мастера")
    
    response = "🎨 Выберите мастера:\n\n"
    for row in rows[1:]:
        if len(row) > 1:
            response += f"👤 {row[1]} ({row[2]})\n"
            response += f"   ⭐ {row[4]} | 💵 {row[7]} руб\n\n"
    
    await message.answer(response)
```

### Записать клиента
```python
@router.message(Command("book"))
async def book_service(message: Message, state: FSMContext):
    config = get_config()
    client = GoogleSheetsClient(
        credentials_file=config.google_credentials_json,
        spreadsheet_id=config.google_spreadsheet_id
    )
    
    # Сохранить данные в БД
    client.append_row("Записи", [
        generate_id(), str(message.from_user.id), message.from_user.first_name,
        '987654321', 'Мастер', 'Услуга', '10.12.2025 14:00', '120', '3500',
        'pending', datetime.now().strftime("%d.%m.%Y %H:%M"), ''
    ])
    
    await message.answer("✅ Запись создана!")
```

### Показать отзывы
```python
@router.message(Command("reviews"))
async def show_reviews(message: Message):
    config = get_config()
    client = GoogleSheetsClient(
        credentials_file=config.google_credentials_json,
        spreadsheet_id=config.google_spreadsheet_id
    )
    
    rows = client.get_sheet_values("Отзывы")
    
    response = "⭐ Отзывы клиентов:\n\n"
    for row in rows[1:]:
        if len(row) > 6:
            response += f"👤 {row[2]} → {row[4]}\n"
            response += f"   {'⭐' * int(row[5])} ({row[5]})\n"
            response += f"   \"{row[6]}\"\n\n"
    
    await message.answer(response)
```

## ✅ Список готовности

- ✅ БД инициализирована
- ✅ Все листы созданы
- ✅ Заголовки добавлены
- ✅ Примеры данных добавлены
- ✅ Документация готова
- ⬜ Добавить больше данных (вручную)
- ⬜ Интегрировать с ботом
- ⬜ Синхронизировать с Calendar

## 📞 Помощь

- **DATABASE_GUIDE.md** - полное руководство
- **DATABASE_READY.md** - инструкции и примеры
- **Код примеров** - в этом файле ☝️

## 🚀 Готово!

БД полностью готова к использованию. Откройте таблицу, добавляйте данные и интегрируйте с ботом!

```
https://docs.google.com/spreadsheets/d/1gU-8cqVOiGHcgY823vQeC6JX8oZfLOrJ_l7OjRqqHiY/edit
```
