# 📊 Полноценная База Данных - ГОТОВО!

## ✅ Что было сделано

Ваша таблица Google Sheets теперь является полноценной структурированной БД со следующей структурой:

### Созданные листы:

1. **Мастера** (10 столбцов)
   - ID, Имя, Специальность, Опыт (лет), Рейтинг, Телефон, Instagram, Цена за сеанс, Статус, Описание
   - Содержит 4 примера мастеров: Алексей, Мария, Иван, Елена

2. **Клиенты** (10 столбцов)
   - ID, Telegram ID, Имя, Телефон, Email, Дата регистрации, Статус, Всего визитов, Общая потрачено (руб), Примечания
   - Содержит 3 примера клиентов: Петр, Ольга, Дмитрий

3. **Записи** (12 столбцов)
   - ID, Telegram ID Клиента, Имя клиента, Telegram ID Мастера, Имя мастера, Услуга, Дата/Время, Длительность (мин), Цена (руб), Статус, Дата создания, Примечания
   - Пуста, готова к использованию

4. **Услуги** (8 столбцов)
   - ID, Название, Описание, Длительность (мин), Базовая цена (руб), Категория, Статус, Изображение URL
   - Содержит 6 услуг: портреты, минимализм, классика, орнаменты и т.д.

5. **Расписание** (9 столбцов)
   - ID, Telegram ID Мастера, Имя мастера, Дата, Время начала, Время окончания, Занято/Свободно, Тип события, Примечания
   - Пуста, готова к синхронизации с Google Calendar

6. **Отзывы** (10 столбцов)
   - ID, Telegram ID Клиента, Имя клиента, Telegram ID Мастера, Имя мастера, Оценка (1-5), Текст отзыва, Дата отзыва, ID записи, Полезно
   - Пуста, готова к использованию

7. **Прайс-лист** (8 столбцов)
   - ID, Мастер, Услуга, Цена (руб), Комиссия (%), Чистый доход, Дата добавления, Активна, Примечания
   - Пуста, готова к использованию

---

## 🚀 Как использовать

### 1. Просмотр БД
Откройте таблицу в Google Sheets по ссылке:
```
https://docs.google.com/spreadsheets/d/1gU-8cqVOiGHcgY823vQeC6JX8oZfLOrJ_l7OjRqqHiY/edit
```

### 2. Добавление новых данных вручную
В каждом листе вы можете добавлять строки непосредственно в Google Sheets.

### 3. Программное добавление данных
```python
from src.db.sheets_client import GoogleSheetsClient
from src.config.config import get_config

config = get_config()
sheets_client = GoogleSheetsClient(
    credentials_file=config.google_credentials_json,
    spreadsheet_id=config.google_spreadsheet_id
)

# Добавить мастера
sheets_client.append_row("Мастера", [
    '5', 'Новый Мастер', 'Тату - Стиль', '7', '4.8', '+7-999-999-99-99', '@instagram', '3500', 'active', 'Описание'
])

# Добавить клиента
sheets_client.append_row("Клиенты", [
    '1004', '111222333', 'Иван Петров', '+7-900-444-55-66', 'ivan@example.com', '03.12.2025', 'active', '0', '0', ''
])
```

### 4. Чтение данных
```python
# Получить все строки из листа
rows = sheets_client.get_sheet_values("Мастера")
for row in rows[1:]:  # Пропускаем заголовок
    print(f"Мастер: {row[1]}, Специальность: {row[2]}")

# Найти строку по значению
row_idx = sheets_client.find_row("Мастера", 0, '1')  # Найти по ID
```

### 5. Обновление данных
```python
# Обновить строку
sheets_client.update_row("Мастера", 2, ['1', 'Новое имя', 'Новая специальность', ...])

# Обновить диапазон
sheets_client.update_range("Мастера", "A2:B10", [[значение1, значение2], ...])
```

### 6. Удаление данных
```python
# Удалить строку
sheets_client.delete_row("Мастера", 2)  # Удалить вторую строку
```

---

## 📝 Важные примечания

### Форматирование
- ✅ Все листы созданы с правильным количеством столбцов
- ✅ Заголовки добавлены (жирный текст, темный фон)
- ⚠️ Если нужно улучшить форматирование, используйте Google Sheets интерфейс напрямую

### ID
- Убедитесь, что ID мастеров и клиентов **уникальны**
- Telegram ID должны быть валидными номерами из Telegram
- Используйте числовые ID для удобства

### Статусы
Используйте следующие значения для полей статуса:

**Клиенты:** `active`, `blocked`, `inactive`
**Мастера:** `active`, `inactive`
**Записи:** `pending`, `confirmed`, `completed`, `cancelled`
**Услуги:** `active`, `inactive`
**Расписание:** `Свободно`, `Занято`, `Перерыв`

### Форматы данных
- **Даты**: `DD.MM.YYYY` (например: `03.12.2025`)
- **Время**: `HH:MM` (например: `14:30`)
- **Телефоны**: `+7-XXX-XXX-XX-XX`
- **Цены**: целые числа или с точкой (например: `3500` или `3500.50`)
- **Рейтинги**: от 0 до 5 с точкой (например: `4.8`)

---

## 🔧 Интеграция с ботом

### Использование сервисов

```python
from src.services.master_service import MasterService
from src.db.sheets_client import GoogleSheetsClient
from src.config.config import get_config

config = get_config()
sheets_client = GoogleSheetsClient(
    credentials_file=config.google_credentials_json,
    spreadsheet_id=config.google_spreadsheet_id
)

# Создать сервис мастеров
master_service = MasterService(sheets_client)

# Получить всех мастеров
masters = master_service.get_all_masters()
for master in masters:
    print(f"{master['id']}: {master['name']} - {master['specialization']}")

# Получить мастера по ID
master = master_service.get_master(1)
if master:
    print(f"Найден: {master['name']}")
```

### Использование в обработчиках бота

```python
# В handlers/master_handler.py
from src.db.sheets_client import GoogleSheetsClient
from src.services.master_service import MasterService
from src.config.config import get_config

async def get_masters_list(message):
    config = get_config()
    sheets_client = GoogleSheetsClient(
        credentials_file=config.google_credentials_json,
        spreadsheet_id=config.google_spreadsheet_id
    )
    
    master_service = MasterService(sheets_client)
    masters = master_service.get_all_masters()
    
    response = "Наши мастера:\n\n"
    for master in masters:
        response += f"🎨 {master['name']}\n"
        response += f"   Специальность: {master['specialization']}\n"
        response += f"   Рейтинг: ⭐ {master['rating']}\n"
        response += f"   Цена: {master['price']} руб\n\n"
    
    await message.answer(response)
```

---

## 📚 Файлы проекта

### Новые файлы
- `src/db/db_initializer.py` - Модуль инициализации БД
- `init_database.py` - Скрипт инициализации структуры БД
- `populate_database.py` - Скрипт заполнения примерами данных
- `init_db.bat` - Батник для Windows
- `init_db.ps1` - PowerShell скрипт
- `DATABASE_GUIDE.md` - Полное руководство по БД

### Обновленные файлы
- `src/db/sheets_client.py` - Исправлен импорт
- `src/services/master_service.py` - Обновлена структура

---

## 🔄 Следующие шаги

1. ✅ Откройте таблицу в Google Sheets и проверьте данные
2. ⬜ Добавьте больше мастеров и клиентов
3. ⬜ Свяжите услуги с мастерами через прайс-лист
4. ⬜ Создайте расписание для мастеров
5. ⬜ Интегрируйте запись клиентов через бот
6. ⬜ Синхронизируйте расписание с Google Calendar

---

## 💡 Советы и рекомендации

### Для удобства работы:
1. **Создайте фильтры** в каждом листе (Google Sheets → Данные → Создать фильтр)
2. **Заморозьте строку заголовков** (Вид → Закрепить → 1 строка)
3. **Используйте условное форматирование** для выделения статусов
4. **Создайте связанные листы** через LOOKUP функции

### Для безопасности:
1. **Регулярно создавайте резервные копии** таблицы
2. **Ограничьте права доступа** в Google Sheets
3. **Не коммитьте credentials.json** в Git
4. **Используйте переменные окружения** для чувствительных данных

---

## 🆘 Возможные проблемы и решения

### Проблема: "Sheet not found"
- **Решение**: Убедитесь, что все листы созданы (запустите `init_database.py`)

### Проблема: "Permission denied"
- **Решение**: Проверьте права доступа сервис-аккаунта в Google Cloud Console

### Проблема: "Timeout"
- **Решение**: Google API имеет лимиты. Если добавляете много данных, добавьте задержку между запросами

### Проблема: "Данные не сохраняются"
- **Решение**: Проверьте подключение интернета и права доступа к таблице

---

## 📞 Контакты

Если у вас есть вопросы по использованию БД, обратитесь к документации или создайте issue в репозитории.

---

**Дата создания**: 3 декабря 2025
**Статус**: ✅ ГОТОВО
**Версия БД**: 1.0

Спасибо за использование системы управления БД для тату-салона! 🎨
