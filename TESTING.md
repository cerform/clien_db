# 🧪 Тестирование и Отладка Telegram Bot

Полное руководство по тестированию бота перед деплоем в Cloud Run.

## 📋 Содержание

1. [Быстрый старт](#быстрый-старт)
2. [Web интерфейс для мониторинга](#web-интерфейс)
3. [Интеграционные тесты](#интеграционные-тесты)
4. [Автоматическая проверка](#автоматическая-проверка)
5. [Troubleshooting](#troubleshooting)

## 🚀 Быстрый старт

### Запуск всех проверок

```bash
# Установите зависимости (если еще не установлены)
pip install -r requirements.txt

# Запустите полную проверку
python run_tests.py
```

Этот скрипт выполнит:
- ✅ Проверку переменных окружения
- ✅ Тест подключения к Telegram API
- ✅ Тест подключения к Google Sheets
- ✅ Тест чтения/записи данных
- ✅ Проверку всех модулей

## 🌐 Web интерфейс для мониторинга

### Запуск Health Check Dashboard

```bash
python health_check.py
```

Затем откройте в браузере: **http://localhost:8080**

### Возможности Dashboard:

1. **Визуальный мониторинг** всех компонентов бота
2. **Автоматическое обновление** каждые 30 секунд
3. **История проверок** с временными метками
4. **Детальная информация** об ошибках с traceback
5. **Статус-бейджи** для быстрой оценки состояния

### Скриншоты функционала:

**Главная страница:**
- Общий статус системы (OK/WARNING/ERROR)
- Карточки для каждого компонента
- Кнопки управления (запуск проверки, авто-обновление)

**Проверки:**
- 🔧 Переменные окружения
- 📦 Модули Python
- 📱 Telegram API
- 🔑 Google Credentials
- 📊 Google Sheets

**API Endpoints:**
- `GET /` - Web интерфейс
- `GET /api/health` - JSON с результатами проверок
- `GET /api/history` - История всех проверок
- `GET /health` - Быстрая проверка для Cloud Run

## 🧪 Интеграционные тесты

### Запуск тестов отдельно

```bash
python test_bot.py
```

### Что тестируется:

#### 1. Environment Variables
Проверка наличия всех необходимых переменных окружения:
- `TELEGRAM_BOT_TOKEN`
- `GOOGLE_SHEETS_SPREADSHEET_ID`
- `GOOGLE_APPLICATION_CREDENTIALS`

#### 2. Telegram API Connection
- Подключение к Telegram API
- Получение информации о боте
- Проверка валидности токена

#### 3. Google Sheets Connection
- Подключение к Google Sheets API
- Проверка прав доступа
- Получение списка листов

#### 4. Google Sheets Read
- Чтение данных из таблицы
- Проверка структуры данных

#### 5. Google Sheets Write
- Запись тестовых данных
- Проверка прав на редактирование

#### 6. Bot Modules
- Загрузка модулей бота
- Проверка импортов

### Пример вывода:

```
🧪 ЗАПУСК ИНТЕГРАЦИОННЫХ ТЕСТОВ TELEGRAM BOT
======================================================================

✅ Environment Variables: Все переменные окружения присутствуют
✅ Telegram API Connection: Подключение успешно. Bot: @YourBot
✅ Google Sheets Connection: Подключение успешно. Таблица: TattooSalon
✅ Google Sheets Read: Чтение успешно. Прочитано 10 колонок
⚠️  Google Sheets Write: Запись не удалась (возможно нет прав)
✅ Bot Modules Loading: Загружено 3 модулей

======================================================================
📊 РЕЗУЛЬТАТЫ ТЕСТИРОВАНИЯ
======================================================================

✅ Пройдено: 5/6
❌ Провалено: 0/6
⚠️  Предупреждений: 1/6

🎉 Все критичные тесты пройдены успешно!
```

## 🔄 Автоматическая проверка

### Запуск перед деплоем

Добавьте в `deploy.sh` проверку перед деплоем:

```bash
# В deploy.sh перед сборкой образа
print_step "Запуск тестов..."
python run_tests.py
if [ $? -ne 0 ]; then
    print_error "Тесты не прошли. Деплой отменен."
    exit 1
fi
```

### CI/CD Integration

Для GitHub Actions создайте `.github/workflows/test.yml`:

```yaml
name: Tests

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.10'
      
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
      
      - name: Run tests
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          GOOGLE_SHEETS_SPREADSHEET_ID: ${{ secrets.GOOGLE_SHEETS_ID }}
        run: |
          python run_tests.py
```

## 🐛 Troubleshooting

### Проблема: "TELEGRAM_BOT_TOKEN не найден"

**Решение:**
```bash
# Проверьте .env файл
cat .env | grep TELEGRAM_BOT_TOKEN

# Или установите напрямую
export TELEGRAM_BOT_TOKEN="your_token_here"
```

### Проблема: "credentials.json не найден"

**Решение:**
```bash
# Проверьте наличие файла
ls -la credentials.json

# Создайте Service Account и скачайте ключ
# См. CLOUD_RUN_DEPLOYMENT.md
```

### Проблема: "Permission denied для Google Sheets"

**Решение:**
1. Откройте Google Sheets таблицу
2. Нажмите "Поделиться"
3. Добавьте email из `credentials.json` (`client_email`)
4. Дайте права "Редактор"

### Проблема: Web интерфейс не открывается

**Решение:**
```bash
# Проверьте, не занят ли порт
lsof -i :8080

# Используйте другой порт
PORT=8081 python health_check.py
```

### Проблема: Timeout при проверке Telegram API

**Решение:**
```bash
# Проверьте интернет-соединение
ping api.telegram.org

# Проверьте прокси/VPN
curl https://api.telegram.org/

# Увеличьте таймаут в health_check.py
```

## 📊 Мониторинг в Cloud Run

### После деплоя

Health check endpoint автоматически доступен:
```
https://your-service-url.run.app/
```

### Cloud Run автоматически использует:
- `GET /health` - для проверки доступности сервиса
- Автоматический restart при ошибках
- Логирование всех проверок

### Просмотр логов в Cloud Run:

```bash
# Все логи
gcloud run services logs read telegram-bot --region us-central1

# Только ошибки
gcloud run services logs read telegram-bot \
    --region us-central1 \
    --log-filter "severity>=ERROR"

# Real-time
gcloud run services logs tail telegram-bot --region us-central1
```

## 🔧 Локальная отладка

### Запуск бота локально с отладкой:

```bash
# 1. Установите все зависимости
pip install -r requirements.txt

# 2. Запустите health check в фоне
python health_check.py &

# 3. Откройте dashboard в браузере
open http://localhost:8080

# 4. Запустите бота в отдельном терминале
python run_bot.py

# 5. Наблюдайте за метриками в реальном времени
```

### Docker отладка:

```bash
# Соберите образ локально
docker build -t telegram-bot-test .

# Запустите с пробросом портов
docker run -p 8080:8080 \
    -e TELEGRAM_BOT_TOKEN="your_token" \
    -e GOOGLE_SHEETS_SPREADSHEET_ID="your_sheet_id" \
    -v $(pwd)/credentials.json:/app/credentials.json \
    telegram-bot-test

# Проверьте health check
curl http://localhost:8080/health
```

## 📈 Метрики и алерты

### Ключевые метрики для мониторинга:

1. **Response Time** - время ответа бота
2. **Error Rate** - процент ошибок
3. **API Availability** - доступность Telegram API
4. **Sheets Access** - успешность операций с Google Sheets
5. **Memory Usage** - использование памяти
6. **CPU Usage** - загрузка процессора

### Настройка алертов в Cloud Run:

```bash
# Создание политики алертов
gcloud alpha monitoring policies create \
    --display-name="Bot Errors Alert" \
    --notification-channels=CHANNEL_ID \
    --condition-threshold-value=5 \
    --condition-threshold-duration=300s
```

## 🎯 Чек-лист перед продакшеном

- [ ] Все тесты проходят успешно
- [ ] Health check показывает статус OK
- [ ] Бот отвечает на команды локально
- [ ] Google Sheets доступен для чтения/записи
- [ ] Service Account имеет все необходимые права
- [ ] Секреты настроены в Secret Manager
- [ ] Dockerfile собирается без ошибок
- [ ] Локальный Docker контейнер запускается
- [ ] Health check endpoint отвечает
- [ ] Логирование настроено корректно

## 🔗 Полезные команды

```bash
# Быстрая проверка всего
python run_tests.py && python health_check.py

# Проверка только критичных компонентов
python -c "from test_bot import BotTester; import asyncio; asyncio.run(BotTester().test_telegram_connection())"

# Экспорт результатов в JSON
python health_check.py --json > health_report.json

# Continuous testing (каждые 5 минут)
watch -n 300 python run_tests.py
```

---

**Готово!** 🎉 Теперь у вас есть полноценная система тестирования и мониторинга бота!
