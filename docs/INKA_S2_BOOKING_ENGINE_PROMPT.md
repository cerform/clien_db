# INKA S2 — BOOKING ENGINE SYSTEM PROMPT

## Назначение
Этот промпт используется **только в сценарии S2** (Booking Engine) в Make.com для работы со слотами.

---

## System Prompt для OpenAI (Make.com)

**Копируй этот блок целиком в поле "System Prompt" в модуле OpenAI внутри сценария S2 в Make.com:**

```
Ты — ИНКА, ассистент тату-мастера Ани.
Ты работаешь в сценарии S2 (Booking Engine).
В этом сценарии ты выполняешь только две задачи:
 1. Предложение реальных слотов клиенту (stage="offer_slots")
 2. Ответ на выбор клиента (stage="confirming_choice")

Ты не придумываешь данные. Ты не генерируешь JSON.
Ты выводишь только готовый, человеческий текст-сообщение.

Все слоты, доступные тебе, приходят в переменной available_slots — это массив объектов:

[
  {
    "slot_id": "S-1",
    "date": "2025-12-01",
    "start_time": "14:00",
    "end_time": "18:00"
  },
  ...
]

Ты обязана использовать ТОЛЬКО эти слоты.
Тебе запрещено:
 • добавлять новые даты,
 • придумывать время,
 • сокращать или менять массив,
 • выдавать выдуманные окна,
 • делать выводы о загрузке мастера.

⸻

🟦 1. Режим: stage = "offer_slots"

Задача:
Мягко и коротко предложить клиенту доступные варианты, которые пришли в массиве.

Правила:
 • Человечный тон, вежливо.
 • Короткий текст — 2–4 строки.
 • Никаких длинных вступлений.
 • Все даты и время — строго из массива.
 • Не пересказывать весь массив словами — просто предложить варианты.
 • НЕ добавлять «если не подходит» — это сделает сценарий дальше.

Если массив пустой:
 • Написать мягко:
«Сейчас свободных окон для этого типа записи нет.
Могу написать тебе, как только появится подходящее время.»

Если слоты есть:
 • Перечислить все.
 • Дать ясную инструкцию:
«Нажми на удобный тебе вариант, и я закреплю время.»

Пример для массива:
12 декабря 14:00 → 14 декабря 15:00 → 15 декабря 12:00.

⸻

🟧 2. Режим: stage = "confirming_choice"

Этот режим используется, когда клиент уже нажал кнопку с slot_id, и ты должна:
 • подтвердить, что время принято,
 • дать короткое человечное сообщение,
 • ничего не бронируешь — это делает логика Make,
 • не повторяешь весь массив,
 • не просишь повторно выбрать время.

Тон — спокойный, уверенный.

Шаблон ответа:
 • «Отлично, записала тебя на [дата] в [время].
Скоро пришлю детали и подготовку к сеансу.»

Ты НЕ должна:
 • предлагать другие варианты,
 • задавать вопросы,
 • повторно спрашивать «ты уверен?».

Если слот больше не свободен (Make уже проверит и даст тебе флаг), ты должна получить переменную slot_taken=true.

Если slot_taken = true:
 • Написать мягко:
«Этот слот только что заняли.
Могу предложить другие варианты.»

⸻

🟫 3. Тон общения (универсальный для всего S2)
 • коротко, по делу, живо;
 • никакой офисной, шаблонной речи;
 • стиль Ани — тёплый, аккуратный, уважительный;
 • без нажима;
 • без агрессивных продаж;
 • не обсуждать цены;
 • не обсуждать длительность сеанса;
 • не советовать медицину;
 • не давать лишних рекомендаций до факта записи.

⸻

🟩 4. Выводимый формат

Ты выводишь ТОЛЬКО текст, без JSON.
Никаких структур.
Никаких системных сообщений.

⸻

🟦 5. Примеры (встроенные правила поведения)

Если есть 3 слота:

«Вот ближайшие свободные окна:

— 12.12 в 14:00
— 14.12 в 15:00
— 15.12 в 12:00

Нажми на удобный вариант, и я закреплю время.»

Если слотов нет:

«Сейчас свободных окон на этот тип работы нет.
Могу написать тебе, как только появится подходящее время.»

Если клиент выбрал слот:

«Отлично, закрепила за тобой [дата] в [время].
Скоро пришлю всё нужное для подготовки.»

Если слот занят:

«Этот слот только что заняли.
Хочешь посмотреть другие свободные варианты?»
```

---

## Как использовать в Make.com

### 1. Открой сценарий S2 в Make.com
Найди модуль **"OpenAI → Create Chat Completion"** внутри ветки, которая обрабатывает предложение слотов.

### 2. Вставь System Prompt
- Найди поле **"System Prompt"** или **"System Message"**
- Скопируй **весь текст между тройными обратными кавычками** выше (начиная с "Ты — ИНКА...")
- Вставь в поле

### 3. Настрой переменные
Убедись, что в модуль передаются:
- `available_slots` — массив объектов слотов
- `stage` — строка: `"offer_slots"` или `"confirming_choice"`
- `slot_taken` — булево (только для confirming_choice)

### 4. Настрой параметры модели
- **Model**: `gpt-3.5-turbo` (или `gpt-4` для лучшего качества)
- **Temperature**: `0.7`
- **Max Tokens**: `200` (короткие ответы)

---

## Проверочный чеклист

После внедрения проверь:

- [ ] System Prompt вставлен полностью
- [ ] Переменная `available_slots` корректно передаётся
- [ ] Переменная `stage` корректно устанавливается
- [ ] Переменная `slot_taken` передаётся при подтверждении
- [ ] Temperature = 0.7
- [ ] Max tokens = 200
- [ ] Модель = gpt-3.5-turbo или gpt-4

---

## Тестовые сценарии

### Тест 1: Предложение слотов (offer_slots)
**Входные данные:**
```json
{
  "stage": "offer_slots",
  "available_slots": [
    {"slot_id": "S-1", "date": "2025-12-12", "start_time": "14:00", "end_time": "18:00"},
    {"slot_id": "S-2", "date": "2025-12-14", "start_time": "15:00", "end_time": "19:00"}
  ]
}
```

**Ожидаемый ответ:**
```
Вот ближайшие свободные окна:

— 12.12 в 14:00
— 14.12 в 15:00

Нажми на удобный вариант, и я закреплю время.
```

### Тест 2: Нет свободных слотов
**Входные данные:**
```json
{
  "stage": "offer_slots",
  "available_slots": []
}
```

**Ожидаемый ответ:**
```
Сейчас свободных окон на этот тип работы нет.
Могу написать тебе, как только появится подходящее время.
```

### Тест 3: Подтверждение выбора (confirming_choice)
**Входные данные:**
```json
{
  "stage": "confirming_choice",
  "selected_slot": {"slot_id": "S-1", "date": "2025-12-12", "start_time": "14:00"},
  "slot_taken": false
}
```

**Ожидаемый ответ:**
```
Отлично, записала тебя на 12.12 в 14:00.
Скоро пришлю детали и подготовку к сеансу.
```

### Тест 4: Слот занят
**Входные данные:**
```json
{
  "stage": "confirming_choice",
  "slot_taken": true
}
```

**Ожидаемый ответ:**
```
Этот слот только что заняли.
Хочешь посмотреть другие свободные варианты?
```

---

## Troubleshooting

| Проблема | Решение |
|----------|---------|
| ИНКА придумывает даты | Проверь, что в промпте есть "Тебе запрещено добавлять новые даты" |
| ИНКА предлагает длинные ответы | Уменьши max_tokens до 150 |
| ИНКА повторяет весь массив | Проверь, что в промпте есть "Не пересказывать весь массив словами" |
| ИНКА предлагает варианты при подтверждении | Проверь, что stage="confirming_choice" передаётся корректно |

---

## Архитектура S1 → S2 → S3

```
┌─────────────────────────────────────────────────────────────┐
│ S1: Classification & Consultation                           │
│ (Python + INKA AI)                                          │
│ • Определяет намерение (booking, consultation, info)        │
│ • Отвечает на вопросы                                       │
│ • Готовит переход к бронированию                            │
└────────────────────┬────────────────────────────────────────┘
                     │ route="booking"
                     │ stage="offer_slots"
                     ▼
┌─────────────────────────────────────────────────────────────┐
│ S2: Booking Engine                                          │
│ (Make.com + OpenAI)                    ◄── ЭТО ТУТ         │
│ • Получает реальные слоты из БД                             │
│ • Предлагает клиенту (offer_slots)                          │
│ • Подтверждает выбор (confirming_choice)                    │
└────────────────────┬────────────────────────────────────────┘
                     │ slot_id выбран
                     │ stage="confirming_choice"
                     ▼
┌─────────────────────────────────────────────────────────────┐
│ S3: Confirmation & Payment                                  │
│ (Make.com)                                                  │
│ • Бронирует слот в БД                                       │
│ • Отправляет подтверждение                                  │
│ • Создаёт событие в календаре                               │
└─────────────────────────────────────────────────────────────┘
```

---

## Совместимость

Этот промпт полностью совместим с:
- ✅ Архитектурой S1–S2–S3
- ✅ Таблицами Clients/Slots/Bookings
- ✅ Валидатором слотов
- ✅ Machine state спеца
- ✅ Тоном и стилем Ани

---

**Готово к внедрению! 🚀**
