# 🎨 INKA AI System - Complete Implementation

**INKA** — это трёхуровневая AI система для вашей тату-студии в Telegram.

Ты, разработчик, получаешь:
- ✅ **Классификатор** (S1) - определяет намерение клиента
- ✅ **Консультант** - отвечает профессионально и теплю
- ✅ **Помощник записи** - ведёт к бронированию
- ✅ **Booking Engine (S2)** - предлагает реальные слоты (NEW!)
- ✅ **Система запретов** - не придумывает данные
- ✅ **Готовые промты** - для Make.com

---

## 📦 Что ты получаешь

### Файлы в проекте

```
src/services/
  ├─ inka_ai.py                      ← Основной модуль (S1)
  ├─ inka_booking_engine.py          ← NEW! S2 Booking Engine
  └─ admin_chat_service.py           ← Интегрирована INKA

docs/
  ├─ INKA_S2_BOOKING_ENGINE_PROMPT.md    ← System Prompt для S2
  ├─ INKA_S2_MAKE_INTEGRATION.md         ← Make.com интеграция S2
  ├─ INKA_MAKE_INTEGRATION.md            ← Полное руководство Make.com
  ├─ INKA_QUICK_START.md                 ← Для разработчиков
  
src/bot/handlers/
  └─ inka_handler.py                 ← Готовый обработчик

examples_s2_booking_engine.py       ← Примеры использования S2
```

### Классы INKA

```python
from src.services.inka_ai import INKA, INKAClassifier, INKAConsultant
from src.services.inka_booking_engine import INKABookingEngine

INKA()                    # Main orchestrator
├─ .classifier            # Route/booking_type/confidence
├─ .consultant            # Warm responses + system prompts
├─ .booking_assistant     # Prepare for S2
└─ .booking_engine        # NEW! S2 Booking Engine

INKAClassifier()          # Stand-alone classifier (no API cost)
INKAConsultant()          # Stand-alone consultant
INKABookingEngine()       # NEW! Stand-alone booking engine (S2)
```

---

## 🚀 Quick Start

### 1. Инициализация

```python
from src.services.inka_ai import INKA

inka = INKA(api_key="sk-...")
```

### 2. Обработка сообщения

```python
result = inka.process(
    message="хочу записаться на тату",
    client_context={
        "has_active_booking": False,
        "client_status": "new"
    }
)

# Результат:
# {
#   "response": "Хорошо, могу показать свободные варианты...",
#   "next_action": "offer_slots",
#   "classification": {
#       "route": "booking",
#       "booking_type": "tattoo",
#       "confidence": 0.85
#   }
# }
```

### 3. Отправить в Telegram

```python
await message.answer(result["response"])

if result["next_action"] == "offer_slots":
    # Включи S2: показ слотов
    await show_booking_slots(...)
```

---

## 🎯 Классификация (маршруты)

Автоматическое определение намерения клиента:

| Route | Примеры | Action |
|-------|---------|--------|
| **booking** | "запишите", "когда есть" | → S2: показ слотов |
| **consultation** | "идея тату", "рефы" | → Ответ консультанта |
| **info** | "больно?", "уход", "цена" | → Ответ консультанта |
| **booking_confirm** | "12-го в 14:00" | → Подтверждение |
| **booking_reschedule** | "перенесите" (есть запись) | → S2: новые слоты |
| **other** | непонятно | → Уточнение |

---

## 💬 Промты для Make.com

### Где вставить

1. Make.com → Scenario → Webhook
2. Find: **System Prompt** field
3. Paste: `inka.consultant.get_system_prompt()`

### Получить промт в коде

```python
inka = INKA(api_key="...")

# Основной промт
prompt = inka.consultant.get_system_prompt()

# Все доступные промты
prompts = inka.get_system_prompts()
# {
#   "consultation_prompt": "...",
#   "info_prompt": "...",
#   "communication_prompt": "...",
#   "general_prompt": "..."
# }
```

### Промт (краткая версия)

```
Ты — ИНКА, персональный ассистент Ани.

🟥 ЗАПРЕТЫ:
✗ Не придумывай даты/слоты/время
✗ Не называй цены без информации
✗ Не давай медицинские советы
✗ Не пиши длинные речи

🟧 ДЕЛАЙ:
✓ Отвечай теплю и профессионально
✓ Задай 1-2 уточняющих вопроса
✓ Предложи посмотреть варианты: "Хочешь посмотреть время?"
✓ Переводи в бронирование мягко
```

**Полный промт** в файле `/docs/INKA_MAKE_INTEGRATION.md`

---

## 🔧 Интеграция в Telegram Bot

### Option A: Использовать готовый обработчик

```python
from src.bot.handlers.inka_handler import create_inka_router

# В startup
dp.include_router(create_inka_router())

# Готово! Любое сообщение будет обработано INKA
```

### Option B: Интегрировать в свой обработчик

```python
from src.bot.handlers.inka_handler import handle_client_message

@router.message()
async def my_handler(message: types.Message, state: FSMContext):
    # Обработать INKA
    result = await handle_client_message(message, state)
    
    # Отправить ответ
    await message.answer(result["response"])
    
    # Проверить следующее действие
    if result["next_action"] == "offer_slots":
        # Показать слоты (S2)
        pass
```

### Option C: Сразу использовать классификатор (без AI)

```python
from src.services.inka_ai import INKAClassifier

classifier = INKAClassifier()
classification = classifier.classify("хочу записаться")

# Быстро, без API запросов! (только правила)
print(classification["route"])  # "booking"
```

---

## 📊 Примеры использования

### Пример 1: Новый клиент хочет записаться

```
CLIENT:  "хочу тату с змеей, когда есть время?"
         ↓
INKA CLASSIFY:
  route: "booking"
  booking_type: "tattoo"
  confidence: 0.90
         ↓
INKA RESPOND: "Хорошо! Могу показать свободные варианты. Хочешь посмотреть?"
         ↓
ACTION: Trigger S2 (show booking slots)
```

### Пример 2: Клиент спрашивает о боли

```
CLIENT:  "Больно ли на рёбрах?"
         ↓
INKA CLASSIFY:
  route: "info"
  confidence: 0.85
         ↓
INKA RESPOND: "Ощущения индивидуальны. Аня подберёт место и подготовит. 
             Где ещё думаешь?"
         ↓
ACTION: Wait for next message
```

### Пример 3: Клиент хочет обсудить дизайн

```
CLIENT:  "У меня идея - геометрия + звёзды. Что думаешь?"
         ↓
INKA CLASSIFY:
  route: "consultation"
  confidence: 0.80
         ↓
INKA RESPOND: "Звучит красиво! Это компактная или большая работа? 
             Есть ли рефы для вдохновения?"
         ↓
ACTION: Continue consultation
```

---

## 🛑 Что INKA НЕ ДЕЛАЕТ

### ❌ Никогда:

```python
# ❌ WRONG - Hardcoding slots
"Свободно 5-го в 14:00, 6-го в 16:00"

# ✅ RIGHT - Transition to S2
"Хочешь посмотреть свободные варианты?"

# ❌ WRONG - Making up prices
"Маленькая тату - 500р, большая - 1500р"

# ✅ RIGHT - Deferring to master
"Стоимость зависит от размера. Аня обсудит."

# ❌ WRONG - Medical advice
"Тату на рёбрах не больно, все нервы притупляются"

# ✅ RIGHT - Personal experience
"Ощущения индивидуальны. Аня подберёт место."
```

---

## 📈 Мониторинг и логирование

### Логирование классификаций

```python
from src.bot.handlers.inka_handler import log_classification

log_classification(
    user_id=123,
    message="хочу записаться",
    classification=result["classification"]
)

# Output:
# [User 123] Message: 'хочу записаться' | 
# Route: booking | 
# Type: tattoo | 
# Confidence: 90%
```

### Проверка результата

```python
result = inka.process(message, context)

print(f"Route: {result['classification']['route']}")
print(f"Confidence: {result['classification']['confidence']:.1%}")
print(f"Response: {result['response']}")
print(f"Next: {result['next_action']}")
```

---

## 🧪 Тестирование

### Тест классификатора

```python
from src.bot.handlers.inka_handler import test_inka_classifier

test_inka_classifier()

# Output:
# ✅ "хочу записаться" → booking
# ✅ "идея тату" → consultation
# ❌ "другое" → wrong result
```

### Тест консультанта

```python
from src.bot.handlers.inka_handler import test_inka_consultant

test_inka_consultant()

# Output:
# ❓ "Больно ли?" → Shows rule-based response
```

### Запуск тестов

```bash
python -m src.bot.handlers.inka_handler test
```

---

## 📚 Документация

### Для разработчиков (Python)
- **[INKA_QUICK_START.md](/docs/INKA_QUICK_START.md)** - Полное руководство интеграции
- **[inka_ai.py](/src/services/inka_ai.py)** - Исходный код с комментариями

### Для Make.com интеграции
- **[INKA_MAKE_INTEGRATION.md](/docs/INKA_MAKE_INTEGRATION.md)** - Пошаговая инструкция
- Готовые примеры сценариев
- Примеры классификаций

### Для админов/владельцев
- **[README.md](/docs/INKA_MAKE_INTEGRATION.md)** - Что INKA может/не может делать
- Примеры диалогов

---

## 🔐 Безопасность и ограничения

### Жёсткие запреты (система не может нарушить):

```python
# CLASSIFIER enforces:
✗ Не создаёт слоты
✗ Не придумывает даты
✗ Не назначает цены
✗ Не обещает то, чего нет

# CONSULTANT enforces:
✗ Не даёт медицинские советы
✗ Не спорит с клиентом
✗ Не пишет лекции (макс 300 tokens)
✗ Не осуждает идеи

# BOOKING_ASSISTANT enforces:
✗ Только переходит к S2
✗ Не выполняет бронирование
✗ Не обновляет календарь
```

---

## ⚙️ Конфигурация

### Переменные окружения

```dotenv
# Уже в .env, используется:
OPENAI_API_KEY=sk-...

# Опционально:
INKA_MODEL=gpt-3.5-turbo  # или gpt-4
INKA_MAX_TOKENS=300
INKA_TEMPERATURE=0.7
```

### Кастомизация

```python
# Добавить свои ключевые слова
classifier = INKAClassifier()
classifier.booking_keywords.extend([
    "запишите",
    "хочу",
    "бронь"
])

# Изменить модель
inka = INKA(api_key="...")
inka.consultant.model = "gpt-4"

# Использовать без API (только правила)
inka = INKA(api_key=None)  # Fallback responses
```

---

## 📞 FAQ

### Q: Могу ли я использовать без OpenAI API?
**A:** Да! Используй `INKAClassifier()` для классификации (правила, быстро, бесплатно). Для консультаций будут fallback-ответы.

### Q: Как часто я плачу за API?
**A:** Только когда отправляешь сообщение через `inka.process()` с API ключом. Классификатор - бесплатен.

### Q: Какой язык поддерживает?
**A:** Русский, английский, иврит, автоматически определяет по сообщению.

### Q: Может ли INKA ошибиться в классификации?
**A:** Да, но редко. Каждая классификация имеет `confidence: 0.0-1.0`. Если низкая - можно игнорировать.

### Q: Где хранится история?
**A:** В памяти (`inka.consultant` хранит историю). Для масштабирования - добавь БД.

### Q: Как перейти на GPT-4?
**A:** `inka.consultant.model = "gpt-4"` перед обработкой.

---

## 🚢 Развёртывание

### Локально (тестирование)

```bash
# Установить зависимости
pip install -r requirements.txt

# Запустить тесты
python -m src.bot.handlers.inka_handler test

# Запустить бот
python run.py
```

### На сервере (production)

```bash
# Убедись:
✓ OPENAI_API_KEY установлен
✓ Telegram BOT_TOKEN установлен
✓ USE_WEBHOOK=true (если нужен)
✓ requirements.txt обновлен

# Запустить
python run.py
```

### На Make.com

```
1. Copy system prompt from inka.get_system_prompts()
2. Paste in Make.com Webhook → System Prompt
3. Call OpenAI with System Prompt + User Message
4. Parse response and route accordingly
```

---

## ✅ Checklist перед продакшеном

- [ ] OpenAI API key установлен и работает
- [ ] Телеграм бот токен установлен
- [ ] Тесты запущены: `test_inka_classifier()` все прошли
- [ ] Make.com промт скопирован правильно
- [ ] Логирование включено для мониторинга
- [ ] Database для хранения истории настроена
- [ ] Fallback-логика (без API) работает
- [ ] Error handling готов
- [ ] Rate limiting проверен

---

## 🎓 Обучение команды

### Для разработчиков:
1. Прочитай `/docs/INKA_QUICK_START.md`
2. Запусти примеры из этого файла
3. Изучи исходный код `/src/services/inka_ai.py`

### Для админов Make.com:
1. Прочитай `/docs/INKA_MAKE_INTEGRATION.md`
2. Скопируй системный промт
3. Протестируй с примерами из документации

### Для тестировщиков:
1. Используй примеры из "Примеры использования" выше
2. Проверь, что нет hardcoded данных в ответах
3. Убедись, что INKA предлагает "показать варианты", а не называет их

---

## 📞 Поддержка

Если что-то не работает:

1. **Проверь API key**: `echo $OPENAI_API_KEY`
2. **Запусти тесты**: `python -m src.bot.handlers.inka_handler test`
3. **Проверь логи**: все ошибки логируются в stderr
4. **Проверь документацию**: 
   - `/docs/INKA_QUICK_START.md`
   - `/docs/INKA_MAKE_INTEGRATION.md`
5. **Используй классификатор** (без API) для отладки

---

## 🎉 Готово!

Твой INKA AI ассистент полностью функционален:

✅ Классификирует интенции клиентов  
✅ Отвечает профессионально  
✅ Ведёт к бронированию  
✅ Соблюдает все запреты  
✅ Поддерживает несколько языков  
✅ Готов к production  

**Начни использовать INKA в своём боте прямо сейчас!**

```python
from src.services.inka_ai import INKA

inka = INKA(api_key="your-key")
result = inka.process("hello, I want a tattoo")
print(result["response"])
```

🚀 **Deploy and enjoy!**
