# 🎨 Улучшения системы букинга

## ✅ Что добавлено

### 1. **Исправлена регистрация телефона**
- Телефон теперь правильно сохраняется в Google Sheets
- Добавлена валидация формата телефона
- Поддержка разных форматов: `+972501234567`, `0501234567`, `972501234567`

### 2. **AI консультант для описания татуировки**
- 🤖 ИИ задаёт уточняющие вопросы о дизайне татуировки
- 📝 Автоматически оценивает необходимое время на работу
- 💬 Ведёт естественный диалог
- 📋 Сохраняет все детали в заметках букинга

**Как использовать:**
1. Клиент нажимает "📅 Book Appointment"
2. Вводит имя и телефон
3. Выбирает "💬 Tell me about your tattoo idea"
4. Описывает идею (или продолжает диалог с AI)
5. AI предлагает предварительное время
6. Клиент выбирает дату и время

### 3. **Гибкая длительность слотов**
- **Старое:** только 1 часовой слот (60 минут)
- **Новое:** настраивается в `.env` файле через `DEFAULT_SLOT_DURATION`

**Варианты:**
```dotenv
DEFAULT_SLOT_DURATION=30    # Консультации
DEFAULT_SLOT_DURATION=60    # Маленькие татуировки
DEFAULT_SLOT_DURATION=90    # Средние работы
DEFAULT_SLOT_DURATION=120   # Большие работы
DEFAULT_SLOT_DURATION=180   # Очень большие работы
```

### 4. **Сохранение данных о татуировке**
- Все заметки клиента о татуировке сохраняются в Google Sheets
- Мастер может видеть всю информацию в "bookings" табе
- Если использовался AI, сохраняется оцениваемое время

---

## 🚀 Как установить

### Установка OpenAI SDK
```bash
pip install -r requirements.txt
```

### Или отдельно
```bash
pip install openai
```

### Конфигурация .env

```dotenv
# Получить ключ на https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-your-actual-key-here

# Стандартная длительность слота (в минутах)
DEFAULT_SLOT_DURATION=120
```

---

## 📋 Поток букинга (обновлённый)

```
1. /start → Главное меню
   ↓
2. "📅 Book Appointment"
   ↓
3. Ввод имени
   ↓
4. Ввод телефона ✅ (ИСПРАВЛЕНО - теперь сохраняется)
   ↓
5. Выбор:
   ├─ "💬 Tell me about your tattoo idea" → AI консультация 🤖
   │   ├─ AI задаёт вопросы
   │   ├─ Оценивает время
   │   └─ Сохраняет всё
   │
   └─ "📋 Just book a consultation" → Прямое бронирование
   ↓
6. Выбор даты 📅
   ↓
7. Выбор мастера 👨‍🎨
   ↓
8. Выбор времени ⏰ (теперь гибкие слоты!)
   ↓
9. Подтверждение ✅
```

---

## 🤖 AI Консультант - Примеры

### Пример 1: Маленькая татуировка
```
Клиент: "Хочу маленький якорь на руке"
AI: "Отличная идея! Якорь - классический дизайн. 
     Сколько сантиметров ты представляешь? 5, 10, 15?
     Чёрно-белый или с цветом?"
Оценка: 30-60 минут
```

### Пример 2: Большая работа
```
Клиент: "Хочу полный рукав на весь предплечье с драконом, пейзажем"
AI: "Вау, звучит эпично! Такие работы требуют времени.
     Сколько цветов? Это будет реалистично или стиль татуировки?"
Оценка: 180-300 минут
```

### Пример 3: Консультация
```
Клиент: "Не знаю, что хочу. Хочу просто консультацию"
AI: "Нет проблем! Мы забронируем консультацию с мастером.
     На консультации он подскажет идеи и варианты.
     Обычно это занимает 30 минут."
```

---

## 📊 В Google Sheets

### Таблица "bookings" - новые колонки:
- `id` - ID бронирования
- `client_id` - ID клиента  
- `master_id` - ID мастера
- `date` - Дата
- `slot_start` - Начало
- `slot_end` - Конец (теперь может быть любая длительность!)
- `status` - Статус (pending/confirmed/cancelled)
- `created_at` - Дата создания
- `google_event_id` - ID события в календаре

### Таблица "clients" - новые колонки:
- `telegram_id` - ID Telegram
- `name` - Имя
- `phone` - **ТЕЛЕФОН ТЕПЕРЬ СОХРАНЯЕТСЯ!** ✅
- `email` - Email
- `notes` - Заметки (содержит описание татуировки из AI)
- `created_at` - Дата регистрации

---

## ⚙️ Запуск бота

```bash
cd tattoo_appointment_bot
python3 run.py
```

Всё готово! 🎉

Если у вас нет API ключа OpenAI, бот будет работать без AI консультанта, но клиенты смогут просто ввести текст о татуировке, и он будет сохранён.
